// const { body } = require('express-validator');


// const validateUserRegistration = [
//   body('course_name').isLength({ min: 3 }).withMessage('Username must be at least 3 characters long'),
//   body('course_duration').isLength({min:1},{max:2}).withMessage('Invalid course Validation'),
//   body('course_fees').isLength({ min: 8 }).withMessage('Invalid course Fees'),
// ];

// module.exports = { validateUserRegistration }


d